{
  "title": "Rebase Coordination",
  "protocol": "rebase_coordination://",
  "classType": ["text"],
  "capabilities": ["noremote", "nosaas"],
  "register": "filter",
  "node": "python",
  "path": "nodes.rebase_coordination",
  "prefix": "rebase_coordination",
  "description": [
    "Deterministic RocketRide execution node for Rebase coordination.",
    "It converts Rebase JSON inputs into typed fingerprint, collision, work-order,",
    "or merge-risk JSON outputs without using an LLM or external API key."
  ],
  "lanes": {
    "text": ["text"],
    "table": ["text"]
  },
  "input": [
    {
      "lane": "text",
      "output": [{ "lane": "text" }]
    },
    {
      "lane": "table",
      "output": [{ "lane": "text" }]
    }
  ],
  "preconfig": {
    "default": "merge-risk",
    "profiles": {
      "fingerprint": { "operation": "fingerprint" },
      "collision": { "operation": "collision" },
      "work-order": { "operation": "work-order" },
      "merge-risk": { "operation": "merge-risk" }
    }
  },
  "fields": {
    "rebase_coordination.operation": {
      "type": "string",
      "title": "Operation",
      "enum": ["fingerprint", "collision", "work-order", "merge-risk"],
      "default": "merge-risk"
    }
  },
  "shape": [
    {
      "section": "Pipe",
      "title": "Rebase Coordination",
      "properties": ["rebase_coordination.operation"]
    }
  ],
  "test": {
    "profiles": ["merge-risk"],
    "outputs": ["text"],
    "cases": [
      {
        "name": "Return typed merge-risk placeholder",
        "text": "{\"episodes\":[],\"workOrders\":[]}",
        "expect": {
          "text": {
            "contains": "\"mergeRisk\""
          }
        }
      }
    ]
  }
}
